print(s: string): nil {
	inline_lua "io.write(c_s)";
}

println(s: string): nil {
	print(s .. "\n");
}

main(): nil {
	print(fib(14));
}

private:

fib(x: int): int {
	if (x >= 0 && x < 2) {
		return x;
	}
	else {
		return fib(x - 1) + fib(x - 2);
	}
}
